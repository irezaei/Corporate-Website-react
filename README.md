# وبسایت شرکتی - Corporate Website

یک وبسایت شرکتی مدرن و حرفه‌ای با استفاده از React و اصول طراحی UX/UI

## ویژگی‌ها

### 🎨 طراحی و UX/UI
- **طراحی مدرن و حرفه‌ای**: استفاده از رنگ‌بندی هماهنگ و فونت‌های مناسب
- **واکنش‌گرا (Responsive)**: سازگار با تمام دستگاه‌ها (موبایل، تبلت، دسکتاپ)
- **انیمیشن‌های نرم**: ترنزیشن‌ها و انیمیشن‌های جذاب برای تجربه کاربری بهتر
- **دسترسی‌پذیری (Accessibility)**: رعایت اصول دسترسی‌پذیری برای همه کاربران

### 🚀 تکنولوژی‌ها
- **React 18**: کتابخانه JavaScript برای ساخت رابط کاربری
- **Vite**: ابزار ساخت سریع و مدرن
- **CSS3**: استایل‌دهی پیشرفته با CSS Variables و Flexbox/Grid

### 📱 بخش‌های وبسایت
1. **Navbar**: منوی ناوبری ثابت با انیمیشن اسکرول
2. **Hero Section**: بخش معرفی با آمار و CTA
3. **About Us**: معرفی شرکت و ارزش‌ها
4. **Services**: نمایش خدمات با کارت‌های تعاملی
5. **Contact**: فرم تماس و اطلاعات ارتباطی
6. **Footer**: فوتر با لینک‌ها و اطلاعات

## نصب و راه‌اندازی

### پیش‌نیازها
- Node.js (نسخه 16 یا بالاتر)
- npm یا yarn

### مراحل نصب

1. نصب وابستگی‌ها:
```bash
npm install
```

2. اجرای پروژه در حالت توسعه:
```bash
npm run dev
```

3. ساخت نسخه Production:
```bash
npm run build
```

4. پیش‌نمایش نسخه Production:
```bash
npm run preview
```

## ساختار پروژه

```
project/
├── public/
├── src/
│   ├── components/
│   │   ├── Navbar.jsx
│   │   ├── Hero.jsx
│   │   ├── About.jsx
│   │   ├── Services.jsx
│   │   ├── Contact.jsx
│   │   └── Footer.jsx
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

## اصول UX/UI پیاده‌سازی شده

### 1. Visual Hierarchy (سلسله مراتب بصری)
- استفاده از اندازه‌های مختلف فونت
- رنگ‌بندی مناسب برای تاکید
- فاصله‌گذاری مناسب (Spacing)

### 2. Consistency (یکپارچگی)
- استفاده از CSS Variables برای رنگ‌ها و اندازه‌ها
- کامپوننت‌های قابل استفاده مجدد
- استایل‌های یکپارچه در تمام بخش‌ها

### 3. Responsive Design (طراحی واکنش‌گرا)
- استفاده از CSS Grid و Flexbox
- Media Queries برای اندازه‌های مختلف صفحه
- منوی موبایل با انیمیشن

### 4. User Feedback (بازخورد کاربر)
- Hover effects روی دکمه‌ها و لینک‌ها
- انیمیشن‌های نرم
- پیام‌های موفقیت در فرم

### 5. Performance (عملکرد)
- استفاده از Vite برای ساخت سریع
- بهینه‌سازی تصاویر و فونت‌ها
- Lazy loading برای بخش‌ها

### 6. Accessibility (دسترسی‌پذیری)
- استفاده از semantic HTML
- برچسب‌های مناسب برای فرم‌ها
- پشتیبانی از keyboard navigation

## سفارشی‌سازی

### تغییر رنگ‌ها
رنگ‌های اصلی در فایل `src/index.css` در بخش `:root` تعریف شده‌اند:

```css
:root {
  --primary-color: #2563eb;
  --secondary-color: #10b981;
  --accent-color: #f59e0b;
  /* ... */
}
```

### تغییر محتوا
محتوا در هر کامپوننت قابل تغییر است. برای مثال:
- `src/components/Hero.jsx`: متن Hero Section
- `src/components/Services.jsx`: لیست خدمات
- `src/components/Contact.jsx`: اطلاعات تماس

## مرورگرهای پشتیبانی شده

- Chrome (آخرین نسخه)
- Firefox (آخرین نسخه)
- Safari (آخرین نسخه)
- Edge (آخرین نسخه)

## مجوز

این پروژه برای استفاده آموزشی و تجاری آزاد است.

## نویسنده

**طراحی و توسعه:** محمدرضا رضائی  
**وبسایت:** [melalweb.com](https://melalweb.com)

ساخته شده با ❤️ برای پروژه طراحی وب

